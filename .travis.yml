language: go
sudo: false
before_install:
  - go get github.com/mattn/goveralls
script:
  - go test -v ./pkg/...
  - go test -v ./pkg/... -covermode=count -coverprofile=profile.cov
  - goveralls -coverprofile=profile.cov -service=travis-ci
